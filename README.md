# Needham–Schroeder Protocol Demo (Symmetric Version)

Демонстрация симметричного протокола Нидхема–Шрёдера на Python.  
Поддерживает несколько клиентов, каждый из которых может быть инициатором или получателем сессии.

## 📁 Структура

- `run.py` — единая точка входа
- `kdc.py` — сервер аутентификации (KDC)
- `client.py` — универсальный клиент
- `crypto.py` — простое шифрование (XOR, только для демонстрации)
- `config.py` — настройки и общие ключи

## ▶️ Запуск

1. **Запустите KDC** (в первом терминале):
   ```bash
   python run.py --role kdc
   ```

2. **Запустите клиентов** (в отдельных терминалах):
   ```bash
   python run.py --role client --name A
   python run.py --role client --name B
   python run.py --role client --name C
   ```

3. **Внутри клиента** введите:
   ```
   connect B
   ```
   чтобы инициировать сессию с клиентом `B`.

## 📝 Поддерживаемые клиенты

По умолчанию: `A`, `B`, `C` (ключи заданы в `config.py`).  
Порты назначаются автоматически: `A → 9000`, `B → 9001`, и т.д.

## ⚠️ Важно

- Шифрование реализовано через XOR — **только для образовательных целей**.
- Не используйте в реальных системах.

## 📦 Зависимости

Только стандартная библиотека Python ≥ 3.7.  
Сторонние пакеты **не требуются**.
